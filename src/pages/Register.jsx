import React from 'react';
import Join from './Join';

/**
 * Register Page
 * Alias of Join component for /register route with referral support
 */
const Register = () => {
    return <Join />;
};

export default Register;
